import 'package:flutter/material.dart';

/// Placeholder for the Core Converter screen (§3).
///
/// Real converter tiles, keypad, and conversion logic are implemented in
/// Batch 03.
class ConverterPage extends StatelessWidget {
  const ConverterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Converter')));
  }
}
