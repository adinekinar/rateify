import 'package:flutter/material.dart';

/// Placeholder for the Trip Budget Tracker list screen (§5).
///
/// Real trip/expense CRUD and budget calculation are implemented in Batch 05.
class TripListPage extends StatelessWidget {
  const TripListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Trip')));
  }
}
